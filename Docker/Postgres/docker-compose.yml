version: '3.8'

services:
  postgres:
    build: .
    container_name: mypg
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  pgdata:
